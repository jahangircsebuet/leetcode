#include<iostream>
#include<vector>

using namespace std;

int main() {
    int arr[] = {4,1,3,2};
    vector<int> v(arr, arr+4);
    for(i=0;i<v.size();i++) {
        v1[v[i]%s] = 0;
    }
    return 0;
}
